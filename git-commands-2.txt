git status check now         to check git folder satus                                              
git init            to check status                                            
git config --global user.name "uname"   to create author name
git config --global user.email souvrajit.chatterjee@gaussnetworks.com
git config user.name "umane"
git config user.email souvrajit.chatterjee@gaussnetworks.com
got add
git commit -m ""
git log
Git diff <filename> 